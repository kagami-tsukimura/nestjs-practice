describe('Practice Jest', () => {
  it('test1', () => {
    const result = 1;
    const expected = 1;
    expect(result).toEqual(expected);
  });

  it('test2', () => {
    const result = 2;
    const expected = 2;
    expect(result).toEqual(expected);
  });
});
